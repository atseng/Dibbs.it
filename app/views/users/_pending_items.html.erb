<div class="span12">
  <table class= "table">
    <tr>
      <th>Avatar</th>
      <th>Item</th>
      <th>Activity</th>
      <th>Category</th>
      <th>Comments</th>
      <th>Value</th>
      <th>Status</th>
    </tr>
      <% @items.each do |item| %>
       <% unless item.dibbs_it.empty? %>
        <tr>
          <td><%= image_tag item.avatar_url(:desktop) if item.avatar? %></td>
          <td><%= link_to item.name, item_path(item), method: :get  %></td>
          <td><%= item.activity %></td>
          <td><%= item.category %></td>
          <td><%= item.description %></td>
          <td><%= item.value %></td>
          <td><%= button_to "Loan it!", url: edit_item_loan_path(item, item.loans), :action => "edit", :remote => true %></td>
          </td>
        </tr>
      <% end %>
    <% end %>
  </table>
</div>
