<div class="container2">
  <div class="row">
    <li><%= link_to "My Items", edit_user_path(current_user) %></li>
    <li><%= link_to "My Friendships", user_friendships_path(current_user) %></li>
    <div class="span12">
      <table class= "table">
        <tr>
          <th>Name</th>
          <th>Email</th>
          <th></th>
        </tr>

        <% @users.each do |user| %>
          <tr>
            <% unless current_user == user %>
              <td><%= user.name %></td>
              <td><%= user.email %></td>
              <td>
               <%= form_for(:friendship, :url => user_friendships_path(:user_id => current_user.id, :friend_id => user.id)) do |f| %>
               <%= f.submit "Friend Request", :method => "post" %>
               <% end %>
             </td>
            <% end %>
          </tr>
        <% end %>
      </table>
    </div>
  </div>
</div>



<%= debug(params) if Rails.env.development? %>
